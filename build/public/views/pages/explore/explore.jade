div(ng-controller='exploreCtrl')
  preload
    #explore(ng-show='ready')
      #filters.col-xs-3(ng-controller='filterCtrl' ng-cloak)
        include filter
      #filterProducts.col-xs-9
        #products
          wrap-big-products(products='exploreProducts')
